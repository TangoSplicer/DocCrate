name: Build DocCrate Studio

on:
  push:
    branches:
      - main
    paths:
      - 'crates/desktop/**'
      - 'crates/core/**'
      - 'ui/**'
      - '.github/workflows/**'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-22.04, windows-latest]
    
    runs-on: ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust Stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustc, cargo

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev build-essential curl wget file libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      # THE NEW SAFETY NET: Run Core Tests Before Building!
      - name: Run Core Logic Tests
        run: cargo test --manifest-path crates/core/Cargo.toml

      - name: Build Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: "crates/desktop"
          includeRelease: true
          includeDebug: false
          releaseId: "Draft Release"
          releaseName: "DocCrate Studio - Latest Build"
          releaseDraft: true
          prerelease: false
