[package]
name = "doccrate-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
walkdir = "2.4"
zip = "0.6"
anyhow = "1.0"
# NEW: Native offline Markdown-to-HTML parser
pulldown-cmark = "0.9"
