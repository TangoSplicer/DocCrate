[package]
name = "doccrate-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0", features = [] }
doccrate-core = { path = "../core" }

[build-dependencies]
tauri-build = "2.0.0"
